{
  "servers": {
    "playwright": {
      "type": "stdio",
      "command": "playwright-mcp",
      "args": ["--headless", "--isolated", "--no-sandbox", "--browser", "chrome", "--output-dir", "/tmp/playwright-mcp"]
    }
  }
}
