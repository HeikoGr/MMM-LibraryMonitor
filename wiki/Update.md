# Update

From the module directory run:

```bash
cd ~/MagicMirror/modules/MMM-LibraryMonitor
git pull
npm install
node --run lint
```

Restart MagicMirror after the update so the backend changes are loaded.